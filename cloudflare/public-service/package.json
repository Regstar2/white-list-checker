{
  "name": "whitelist-monitor-tg-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json",
    "lint": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json",
    "test": "vitest run",
    "build": "wrangler deploy src/index.ts --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "migrations:local": "wrangler d1 migrations apply DB --local",
    "migrations:remote:list": "wrangler d1 migrations list DB --remote"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.19.0",
    "@cloudflare/workers-types": "^5.20260729.1",
    "typescript": "^5.9.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.115.0"
  }
}
